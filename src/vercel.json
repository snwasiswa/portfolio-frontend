{
  "buildCommand": "npm run build -- --configuration production && echo '{\"apiUrl\":\"'$API_URL'\"}' > dist/portfolio-frontend/config.json",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
